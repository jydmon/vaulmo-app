# Development environment values. Copy to .env at the repo root for docker compose,
# or into apps/api/.env for running the API directly. DEV SECRETS ONLY.
POSTGRES_USER=lifehub
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=lifehub_dev

JWT_ACCESS_SECRET=dev-access-secret-change-me
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
MFA_ISSUER=LifeHub
STORAGE_DRIVER=local
LOG_LEVEL=info

SUPERADMIN_EMAIL=admin@lifehub.local
SUPERADMIN_PASSWORD=ChangeMe123!

# Phase 8/9 (dev)
EMERGENCY_ACCESS_ENABLED=false
