# Proof — captured end-to-end run

Generated by `npm run test:e2e` in `apps/api` against a real PostgreSQL 16 database.
This is the automated proof of the Phase 1 objective: users can securely register,
authenticate (with MFA) and access the platform, with RBAC, audit logging and file storage.

```
HEALTH & MONITORING
  ✓ GET /livez is ok
  ✓ GET /readyz reports DB up
  ✓ GET /metrics exposes gauges
REGISTRATION
  ✓ register returns 201
  ✓ register issues access + refresh tokens
  ✓ register creates a tenant for the user
  ✓ duplicate email is rejected (409)
  ✓ weak password is rejected (422)
PROFILE & RBAC (role: tenant_owner)
  ✓ GET /me returns the profile
  ✓ user has tenant_owner role
  ✓ user has file:write permission
  ✓ tenant_owner is DENIED platform admin (403)
MFA ENROLMENT (TOTP)
  ✓ enroll returns a secret + QR
  ✓ confirm enables MFA + returns recovery codes
AUTHENTICATION WITH MFA
  ✓ login now requires MFA (challenge issued)
  ✓ wrong MFA code is rejected (401)
  ✓ correct MFA completes login
  ✓ a one-time recovery code also completes login
SECURE FILE STORAGE (tenant-scoped)
  ✓ init-upload returns a presigned URL
  ✓ upload stores the file + checksum
  ✓ file appears in the tenant vault
SUPER ADMIN (platform role)
  ✓ super admin can log in
  ✓ super admin CAN read all tenants (200)
  ✓ platform sees the newly registered tenant
  ✓ super admin metrics report users > 0
AUDIT TRAIL (append-only)
  ✓ audit captured registration
  ✓ audit captured successful login
  ✓ audit captured MFA enablement
  ✓ audit captured file upload
  ✓ audit captured the RBAC denial
SESSION LIFECYCLE
  ✓ refresh token rotates to a new pair
  ✓ old refresh token cannot be reused
BRUTE-FORCE PROTECTION
  ✓ repeated bad passwords lock the account (423)
================================================
  RESULT: 33 passed, 0 failed
================================================
```
